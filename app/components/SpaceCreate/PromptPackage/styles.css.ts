import { appStyles } from "~/styles/layer.css";

export const promptPackageStyle = appStyles({
  label: {
    display: "inherit",
    gap: "8px",
    marginBottom: "24px",
    width: "100%",
  },
});
