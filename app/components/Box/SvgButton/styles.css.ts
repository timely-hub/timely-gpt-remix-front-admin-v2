import { appStyles } from "~/styles/layer.css";

export const svgButtonStyle = appStyles({
  wrap: {
    display: "inline-flex",
    justifyContent: "center",
    alignItems: "center",
    cursor: "pointer",
  },
});
